// Dir
import { isDir } from './dir/is';

// File
import { isFile } from './file/is';
import { fileStats } from './file/stats';

export {
    // Dir
    isDir,
    // File
    isFile,
    fileStats
};
